
Project Related
Onboarding Forms

If We need to Use PDF then we need to provision imagick on docker

```docker
RUN apt-get -y update && \
    apt-get install pkg-config libmagickwand-dev -y && \
    pecl install imagick

RUN docker-php-ext-enable imagick
```

also if we need to upload video

```
composer require php-ffmpeg/php-ffmpeg
```

Fix Login For Multi Auth
Employee and Client Login Page 
Employee and Client Password Reset 
Employee and Client Email

Dashboard For User 
(Employee Management), 
(Client Management) , 
(Project Management) , 
(Campaign Management) , 
(Task Management) , 
(Subtask), 
(File), 
(Comment) , 
(Activity)
Dashboard for Employee (Project Management) 
Dashboard for Client (View Campaigns) Upload Photo , Change Password

Password reset blade
Fix The Nav and Sidebar
Fix Vue error
